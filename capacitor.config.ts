import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'chat-video-call-test-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
